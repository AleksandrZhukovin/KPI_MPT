name: Docker Image Build

on:
  push:
    branches: [ "master" ]

jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build and push the image
        run: docker login --username AleksandrZhukovin --password ${{ secrets.PASSWORD }}
             docker build -t ghcr.io/aleksandrzhukovin/lab5 -f lab5/Dockerfile .
             docker push ghcr.io/aleksandrzhukovin/lab5